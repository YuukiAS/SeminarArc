---
id: 0.2.x_reconstruction_workspace_viewmodel
status: completed
executor: codex
completed_at: 2026-08-30
---

# 0.2.x Reconstruction Workspace ViewModel Result

## Summary

Implemented the UI-facing state foundation for Research Reconstruction without adding the Compose screen yet.

## Delivered

- Added `ReconstructionWorkspaceUiState`, `ReconstructionAssetUiItem`, `OcrStatusFilter`, and one-shot workspace events.
- Added `ReconstructionWorkspaceViewModel` for a single seminar.
- Combined photo assets, OCR results, processing jobs, KEY_SLIDE tag ids, search query, OCR status filter, and key-slide-only filter into immutable UI state.
- Added repository support for observing asset ids by system tag.
- Added repository support for editing OCR text while preserving the original recognized text.
- Exposed ViewModel actions for key-slide toggle, OCR editing, image enhancement, and local OCR.
- Added `ReconstructionWorkspaceRoute` for the upcoming Compose screen.

## Verification

- `./gradlew -Dkotlin.compiler.execution.strategy=in-process testDebugUnitTest assembleDebug lintDebug compileDebugAndroidTestKotlin`

The command was run from the WSL `/tmp/SeminarArc-main-latest` working clone with `ANDROID_USER_HOME=/tmp/seminararc-android-home` and `GRADLE_USER_HOME=/tmp/seminararc-gradle`.

## Not Done

- Compose workspace screen and navigation entry are not wired yet.
- Worker queue integration for OCR/enhancement is not wired yet.
- Windows Emulator connected regression and UI smoke were not run in this WSL session.
