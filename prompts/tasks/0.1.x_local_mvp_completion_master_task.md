---
id: 0.1.x_local_mvp_completion_master
status: in_progress
owner: codex
created: 2026-08-29
allow_code_change: true
allow_shell_command: true
allow_network: true
allow_external_upload: false
requires_human_approval: false
---

# 0.1.x Local MVP Completion Master Task

## 目标

从 `e195192 feat: add full recording playback` 开始，连续完成 `0.1.2` 设备验收、`0.1.3` Capture + Timeline、`0.1.4` Clip、`0.1.5` Export + Local MVP Acceptance，直到形成可用于线下 seminar 的本地优先 MVP。

## 执行规则

- 按阶段创建内部 `prompts/tasks/<id>_task.md` 与 `<id>_result.md`。
- 每个阶段保持可审查 diff，验证通过后 commit + push `main`。
- 不需要每完成一个内部 task 就等待用户重新授权。
- 不跳过 gate；如果遇到 master 附件定义的 hard blocker，记录证据后停止。

## 当前阶段

首先执行 `0.1.2_recording_validation_and_docs_task.md`。

`0.1.2` hard gate：必须完成设备级 recording 主闭环验证，或者证明当前环境没有可访问 Android device/emulator 且无法恢复设备连接。不能跳过该 gate 进入 `0.1.3`。

## 明确禁止

- 不进入 `0.2.x`。
- 不实现 OCR、formula OCR、Whisper、transcription、AI summary、paper lookup、DOI、Notion、cloud provider、login、sync、ads、payment、analytics。
- 不上传用户数据、录音、照片、日志、凭据或测试媒体。
- 不提交 APK、录音、照片、导出 ZIP、私人媒体、build artifacts、本地配置或凭据。
