---
id: 0.2.x_reconstruction_schema_design
status: completed
owner: codex
created: 2026-08-30
allow_code_change: false
allow_shell_command: true
allow_network: false
allow_external_upload: false
requires_human_approval: false
---

# 0.2.x Reconstruction Schema Design Task

## 目标

审计当前 Room schema，并为 `0.2.x` Local Visual Reconstruction 设计非破坏性 migration。

## 输入

- `AppDatabase` 当前 version。
- `app/schemas/com.yuukias.seminararc.data.local.AppDatabase/2.json`。
- 现有 `seminars`、`recordings`、`timeline_events`、`audio_clips` entities/DAO/repository。
- `docs/plans/0.2.x-reconstruction-readiness-plan.md`。

## 输出

- 写回 `prompts/tasks/0.2.x_reconstruction_schema_design_result.md`。
- 明确保留表、新增表、backfill、migration version、失败保护和 instrumentation 策略。

## 禁止

- 不改 production schema。
- 不做 destructive migration。
- 不实现 `0.3.x` transcript/reference provider。
