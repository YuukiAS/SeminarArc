---
id: 0.1.5_local_mvp_export_acceptance
status: completed_headless
owner: codex
created: 2026-08-29
allow_code_change: true
allow_shell_command: true
allow_network: true
allow_external_upload: false
requires_human_approval: false
---

# 0.1.5 Local MVP Export Acceptance Task

## 目标

按 `docs/plans/0.1.5-local-mvp-export-acceptance-plan.md` 完成本地优先 MVP 的 Markdown/ZIP 导出、Android 导出入口、文档和验收记录。

## 范围

- 实现 UI 无关的 export domain model、assembler、Markdown renderer。
- 实现单 seminar Markdown 导出和 ZIP 导出，ZIP 布局为 `<seminar-slug>/seminar.md` 与 `media/abstract|photos|clips/`。
- 实现 `ACTION_CREATE_DOCUMENT` Markdown/ZIP 保存和 Android share sheet。
- 覆盖 JVM golden tests：中文、英文、Unicode、Markdown 特殊字符、LaTeX-like 文本。
- 覆盖 ZIP structure、missing media skip、relative links。
- 更新 README、ARCHITECTURE、PRIVACY、CHANGELOG 和 result/acceptance 记录。

## 禁止事项

- 不实现 Notion API、cloud upload、OCR、transcription、summary/provider processing、cross-device sync、ads、subscriptions、payments、analytics、release signing。
- 不提交导出 ZIP、测试录音、测试照片、私人媒体或 build artifacts。
- 不断开、重启、重置真机；不删除或移动真机用户内容。

## 验证要求

- `./gradlew testDebugUnitTest assembleDebug lintDebug`
- `git diff --check`

## 完成条件

- 0.1.5 headless gate 通过。
- 写入 `prompts/tasks/0.1.5_local_mvp_export_acceptance_result.md`。
- 写入 master result，明确真实设备 end-to-end acceptance 是否已执行。
- commit + push `main`。
