# 0.4.x Notion-ready Save Entry Result

## Status

Implemented.

## Changes

- `SeminarExportRepository` now exposes `writeNotionReadyMarkdown`.
- `SeminarExportRepositoryImpl` writes `SeminarNotionReadyRenderer.renderMarkdownPreview` output to a user-selected document URI.
- Seminar Detail export controls now include `Save Notion-ready Markdown`.
- The existing Compose instrumentation test fixture now verifies that the save and share Notion-ready controls are visible.

## Privacy / Scope Boundary

- The feature is local and user-triggered.
- The saved content is Markdown preview text generated from existing local export data.
- No Notion OAuth, API client, token, backend, network call, upload, or provider runtime is added.

## Verification

- `.\gradlew.bat --no-daemon testDebugUnitTest compileDebugAndroidTestKotlin`: passed.
- `.\gradlew.bat --no-daemon lintDebug assembleInternal`: passed.
- `D:\Android\Sdk\build-tools\36.0.0\apksigner.bat verify --verbose --print-certs app\build\outputs\apk\internal\app-internal.apk`: passed.
- Internal signer certificate SHA-256 digest: `7555bf92fb796c6209f353f66c156c6f65e0312f18ee918557b572014b2db28a`.
- Internal APK SHA-256: `69f4e1ef59628654f3beba208dfb0426c1b4698951b3458196f2f7f3e0b802b8`.
- Internal APK size: `60485794` bytes.
- Refreshed ignored local staging artifact `release\SeminarArc-0.3.1-internal.1.apk` and `.sha256`.
- C-drive root cache check: `C:\.gradle` and `C:\.android` were absent; D-drive local Gradle/Android homes were present.
- Narrow secret scan found only expected Gradle signing property key names in `app\build.gradle.kts`; no secret values or private key material were found.

## Remaining Gate

- Connected Android tests and device installation remain intentionally skipped while Windows ADB can see protected physical device serial `8cc54656`.
