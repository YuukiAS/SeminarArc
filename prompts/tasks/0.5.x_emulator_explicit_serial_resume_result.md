---
task_key: 0.5.x_emulator_explicit_serial_resume
completed_at: 2026-09-10
status: complete
owner: codex
---

# Result

`0.5.x` Windows Emulator closeout passed through the task-authorized mixed-inventory explicit-emulator lane. No physical-device operation was performed.

## Device Inventory

- Final Windows ADB inventory: `8cc54656 unauthorized` and `emulator-5554 device product:sdk_gphone64_x86_64 model:sdk_gphone64_x86_64 device:emu64xa`.
- Explicit emulator serial used for every non-inventory ADB command: `emulator-5554`.
- Physical device received any command: `NO`.

## Local Gates

- `testDebugUnitTest assembleDebug assembleInternal compileDebugAndroidTestKotlin assembleDebugAndroidTest lintDebug`: PASS.
- Default release metadata after closeout: `versionName = 0.5.0-alpha.1`, `versionCode = 50001`.
- Debug APK package: `com.yuukias.seminararc`.
- Internal APK package: `com.yuukias.seminararc.internal`.
- Test APK package: `com.yuukias.seminararc.test`.
- Instrumentation runner: `com.yuukias.seminararc.SeminarArcTestRunner`, target package `com.yuukias.seminararc`.

## Explicit Instrumentation

The APKs were installed only with:

```text
adb -s emulator-5554 install -r app-debug.apk
adb -s emulator-5554 install -r app-debug-androidTest.apk
adb -s emulator-5554 shell am instrument -w -r com.yuukias.seminararc.test/com.yuukias.seminararc.SeminarArcTestRunner
```

Result:

```text
OK (24 tests)
```

Covered Android instrumentation included Room migrations/current schema regression, DAO regression, ML Kit OCR smoke, Android Bitmap enhancement smoke, WorkManager processing queue regression, Reconstruction workspace 0.5 formula region/overlay/drag/provider UI regression, Reference Review regression, Detail export-action regression and Library UI regression.

## Internal APK Smoke

- Preserved old baseline APK: `build/release-smoke/SeminarArc-0.3.1-internal.1-base.apk`.
- Old baseline installed on emulator: `com.yuukias.seminararc.internal`, `versionCode=30101`, `versionName=0.3.1-internal.1`.
- New internal APK update-in-place install: PASS.
- New package inspection on emulator: `versionCode=50001`, `versionName=0.5.0-alpha.1`, same appId.
- Launcher resolved to `com.yuukias.seminararc.internal/com.yuukias.seminararc.MainActivity`.
- Launch smoke: PASS; process remained alive after startup.
- Recent scoped logcat check: no `FATAL EXCEPTION` or `AndroidRuntime` crash for the package.

## Signature / Hash / Secret Scan

- APK signer certificate SHA-256: `7555bf92fb796c6209f353f66c156c6f65e0312f18ee918557b572014b2db28a`.
- Release APK staged as ignored local artifact: `release/SeminarArc-0.5.0-alpha.1.apk`.
- APK SHA-256: `F40DC7B0EE102CBB8B523C9CA0B6C1758F5263AC646F2079C23C6968F0BFDBED`.
- High-risk secret scan excluding build/cache/release artifacts and `.agents` example material: PASS. The broad scan only found expected documentation/key-name/example text, not keystore material, password values, API tokens or private keys.

## Policy / Status

- Updated testing policy: preferred path is emulator-only inventory with ordinary connected Gradle task; fallback is mixed inventory with protected physical serial, no unscoped connected Gradle task, and task-authorized explicit `adb -s <emulator>` install + `am instrument`.
- `0.5.x COMPLETE`: YES, for approved local-safe alpha scope.
- Remaining deferred provider paths: live Mathpix, PaddleOCR/pix2tex or any cloud formula OCR provider require future credential/backend/license approval.

## Release State

- Ready to commit, push `main`, tag `v0.5.0-alpha.1`, run version CI and publish GitHub prerelease with:
  - `SeminarArc-0.5.0-alpha.1.apk`
  - `SeminarArc-0.5.0-alpha.1.apk.sha256`
