---
id: 0.5.x_formula_schema_domain
status: complete
owner: codex
created: 2026-09-08
---

# 0.5.x Formula Schema Domain Result

## Status

`COMPLETE`

## Changes

- Room schema bumped from version `6` to version `7`.
- Added `formula_regions` for seminar-owned, user-selected photo regions with normalized crop coordinates, source asset/path provenance, rotation, optional label, and timestamps.
- Added `formula_results` for provider-independent LaTeX output with state, provider id/version, confidence, edited flag, error message, provenance JSON, and timestamps.
- Added `FormulaDao` observe/get/insert/update/delete methods for local formula foundation.
- Added `FormulaRegion`, `FormulaResultState`, and `FormulaResult` domain models.
- Extended `ProcessingJobType` with `FORMULA_OCR`.
- Kept `FORMULA_OCR` safe in existing processing runtime: Worker fails it as not wired yet, and WorkManager recovery/retry does not auto-enqueue it until the later provider queue task implements the real path.
- Added `MIGRATION_6_7` and checked in Room schema `app/schemas/com.yuukias.seminararc.data.local.AppDatabase/7.json`.
- Added migration compile coverage in `AppDatabaseMigrationTest`.

## Verification

- `testDebugUnitTest compileDebugAndroidTestKotlin` passed on Windows native Codex with `JAVA_HOME`, `GRADLE_USER_HOME`, `ANDROID_PREFS_ROOT`, and Gradle JVM `user.home` rooted on `D:\`.
- `lintDebug assembleInternal` passed with the same D-rooted environment.
- Internal APK package inspection: `applicationId = com.yuukias.seminararc.internal`, `versionName = 0.3.1-internal.1`, `versionCode = 30101`, `minSdk = 26`, `targetSdk = 36`.
- APK signature verification passed with internal dogfood certificate SHA-256 `7555bf92fb796c6209f353f66c156c6f65e0312f18ee918557b572014b2db28a`.
- Internal APK SHA-256: `B9B91FCA37D08A29BB7221A6F3871B8E41E6298F8F6085E05AB124A106696547`.
- C-drive cache check stayed clean for `C:\.gradle` and `C:\.android`; Gradle/Android homes exist under `D:\Code\SeminarArc-emulator`.
- Secret scan found only non-secret Gradle signing property key names and historical documentation path strings; no keystore material, password value, API token, or private key was found.

## Deferred

- Connected `MIGRATION_6_7` execution on Windows Emulator is deferred until Windows ADB no longer sees protected physical serial `8cc54656`.
- Formula provider contract, fake/manual provider, formula region UI, durable formula processing queue, and BibTeX/RIS export remain next tasks.
