---
id: 0.5.x_deterministic_photo_fixture_closeout
status: blocked_emulator_camera_fixture
owner: codex
created: 2026-09-13
completed: 2026-09-13
---

# 0.5.x Deterministic Photo Fixture Closeout Result

## Summary

This task added selective black-box scenario execution and a repo-safe deterministic slide fixture generator, then performed the bounded B04/B07 focused closeout against two official Android Emulator camera fixture mechanisms.

Both official mechanisms failed at the same black-box boundary: SeminarArc reached `Capture Slide`, but CameraX did not report a terminal saved-photo state within the bounded wait. Per task policy, no third camera approach was attempted.

## Selective Run Entry

```powershell
.\scripts\run-blackbox-acceptance.ps1 -Scenarios B04,B07 -CameraFixturePath (Resolve-Path .\private\blackbox-fixtures\seminararc-camera-fixture.png).Path
```

Default behavior remains the full B01-B08 catalog when `-Scenarios` is omitted.

## Fixture

- Generator: `scripts/blackbox/New-DeterministicCameraFixture.ps1`
- Runtime output: `private/blackbox-fixtures/seminararc-camera-fixture.png`
- Runtime output is ignored by Git; the deterministic generator is committed.
- Fixture content:
  - title `SeminarArc Camera Fixture`
  - English body text
  - Chinese body text
  - DOI clue `10.1038/nature12373`
  - formula `E = mc^2`
  - formula crop target `y = X beta + epsilon`
  - simple bar chart / geometry region

## Emulator Camera Mechanisms Tried

Current Emulator inventory / binary:

- Emulator: `37.1.11.0`
- AVD: `Pixel_8`
- Serial: `emulator-5554`
- ADB inventory showed only `emulator-5554 device`

Official camera mechanisms checked from local Emulator help:

- `-camera-back imagefile:<filename>`
- `-camera-back environment -virtualscene-poster wall=<filename>`

Attempt 1:

```powershell
emulator.exe -avd Pixel_8 -camera-back imagefile:<fixture-path> -camera-front emulated -no-snapshot-save
```

Evidence:

- `private/blackbox-acceptance/20260913T140752Z`
- Selected scenarios: B04, B07
- Focused steps: 14
- B04: `BLOCKED_EMULATOR_CAMERA_FIXTURE`
- B07: `BLOCKED_EMULATOR_CAMERA_FIXTURE`

Attempt 2:

```powershell
emulator.exe -avd Pixel_8 -camera-back environment -virtualscene-poster wall=<fixture-path> -camera-front emulated -no-snapshot-save
```

Evidence:

- `private/blackbox-acceptance/20260913T141242Z`
- Selected scenarios: B04, B07
- Focused steps: 14
- B04: `BLOCKED_EMULATOR_CAMERA_FIXTURE`
- B07: `BLOCKED_EMULATOR_CAMERA_FIXTURE`

## Scenario Status

| Scenario | Final status | Focused steps | Details |
| --- | --- | ---: | --- |
| B04 | `BLOCKED_EMULATOR_CAMERA_FIXTURE` | 5 per attempt | CameraX did not report a terminal photo capture state for the deterministic camera fixture. |
| B07 | `BLOCKED_EMULATOR_CAMERA_FIXTURE` | 6 per attempt | B07 formula path still depends on a saved photo asset; focused setup hit the same CameraX fixture blocker. |

Total focused harness steps across the two official approaches: 28.

Approximate focused runtime:

- Attempt 1 harness: about 3m23s.
- Attempt 2 harness: about 3m10s.
- Including Emulator restarts and boot waits: about 9 minutes.

## Changes Made

- `scripts/run-blackbox-acceptance.ps1`
  - Added `-Scenarios` selective execution.
  - Added focused B04/B07 UI setup that creates a synthetic seminar, starts photos-only mode, captures through CameraX, and completes the seminar without Room injection or test-only backdoors.
  - Made B07 reopen its own reconstruction route instead of relying on the previous navigation stack.
  - Made B07 safe when B05 was not part of the selected scenario set.
  - Added minimal export behavior for focused selective runs.
  - Added selected scenario / camera fixture metadata to summary output.
- `scripts/blackbox/New-DeterministicCameraFixture.ps1`
  - Added deterministic synthetic PNG generator.
- `.gitignore`
  - Ignored `private/blackbox-fixtures/` runtime PNG output.

Production app code was not modified.

## Bugs Fixed

No production bug was fixed in this closeout.

Harness bugs fixed:

- Full-catalog-only execution made B04/B07 closeout too expensive.
- B07 depended on prior B06 navigation state and B05 result presence.
- Runtime deterministic fixture files were not ignored before this task.
- PowerShell fixture generation needed ASCII-safe Chinese text handling for Windows PowerShell 5.

## Validation

Completed:

- Read required task, policy, result, review and harness files.
- Verified required commits `85d6ebd` and `31a85c5` are contained in `HEAD`.
- Verified Emulator help supports `imagefile:<filename>` and `-virtualscene-poster wall=<filename>`.
- Generated and visually inspected the synthetic fixture.
- Ran focused B04/B07 with both official camera mechanisms.
- `git diff --check`: PASS.

## Next Step Decision

- Real-world media corpus: not yet for CameraX black-box capture. The same Emulator camera fixture boundary must be resolved or explicitly moved to a separate Emulator/AVD infrastructure task.
- Visual regression: not recommended yet for B04/B07, because the deterministic photo state is still not reachable through black-box CameraX capture.
- `0.5.1-alpha`: not recommended. This task changed harness infrastructure only and did not close the photo blocker or fix a production P0/P1 bug.
