---
id: 0.1.x_local_mvp_completion_master
status: implementation_complete_physical_acceptance_deferred
owner: codex
updated: 2026-08-30
---

# 0.1.x Local MVP Completion Master Result

## 阶段结果

- `0.1.2` recording validation/docs：主录音闭环已在 GM1910 上做过真实验证，commit `95061d7 test: validate 0.1.2 recording gate` 已推送。
- `0.1.3` capture/timeline：CameraX capture、timeline event、review route 和 playback offset 入口完成，commit `6a4f59b feat: add 0.1.3 capture timeline` 已推送。
- `0.1.4` clip playback resilience：WorkManager clip generation、retry、ready playback 和 full-recording fallback 完成，commit `7d66ade feat: add 0.1.4 clip generation` 已推送。
- `0.1.5` local export acceptance：Markdown/ZIP export、Android save/share surface、docs 和 export tests 完成，commit `1a2f967 feat: add local seminar export` 已推送。
- `0.1.5` follow-up migration fix：真机 smoke 发现旧 Room version 1 数据库启动崩溃，已新增 `MIGRATION_1_2`、schema `2.json` 和 migration instrumentation test，commit `02e7801 fix: add room migration for clip retry count` 已推送。

## 已验证

- 各阶段代码均按 task/result 文件记录。
- 当前实现线已通过 `./gradlew testDebugUnitTest assembleDebug lintDebug` 和 `git diff --check`。
- `0.1.2` 曾在 protected physical device `8cc54656` / GM1910 上通过真实 recording、background/lockscreen、notification return、End Seminar、`.m4a` 和 full playback 主闭环。
- 后续一次 `connectedDebugAndroidTest` migration instrumentation 安装阶段触发远程真机连接安全事件：设备从 WSL ADB 消失，usbipd 状态退回 `Shared (forced)`；用户随后手动恢复 attach。
- 禁止范围保持未实现：OCR、转写、AI、Notion、cloud sync、login、ads、subscriptions、payments、analytics。

## 测试策略调整

2026-08-30 起，用户决定采用 **Emulator-first + protected physical-device smoke**：

- Windows Android SDK：`D:\Android\Sdk`。
- Windows Android Studio / Emulator 承担 connected/instrumentation/migration/Compose UI regression。
- WSL canonical repo 继续承担开发、JVM tests、build 和 lint。
- GM1910 不再作为日常 connected/instrumentation target，只在未来明确授权的 hardware acceptance checkpoint 做少量真实硬件 smoke。
- 原 `0.1.x_local_mvp_device_acceptance_resume_task.md` 已 supersede，不再为了关闭 master goal 立即跑完整真机 E2E。
- 当前有效 task 是 `0.1.x_emulator_onboarding_and_regression_task.md`；2026-08-30 首次执行结果为 `EMULATOR_INFRA_NEEDS_FIX`，原因是当前 WSL session 无法执行 Windows interop 且 `/mnt/d` 只读，尚不能建立 Windows mirror 或验证 Emulator。该 task 开始时 WSL physical preflight 未见 GM1910，结束 postflight 只读枚举显示 `8cc54656 device`；已停止继续操作真机。

详细规则见 `AGENTS.md` 与 `docs/DEVICE_TESTING.md`。

## 当前结论

- `0.1.x` 功能实现线：**COMPLETE**。
- WSL headless gate：**PASS**。
- 物理真机最终全量 acceptance：**DEFERRED BY TEST STRATEGY**，不再作为日常开发 blocker。
- Emulator regression：**PENDING / EMULATOR_INFRA_NEEDS_FIX**。
- deletion cleanup 的真实设备验收：**DEFERRED / REQUIRES EXPLICIT HUMAN APPROVAL**。

这不是 `FULL RELEASE ACCEPTANCE`，但也不再标记为 `FAIL / DEVICE_EVIDENCE_MISSING` 或 stalled。后续先完成 Emulator onboarding/regression，再根据实际产品阶段决定是否安排新的真机 hardware checkpoint。
