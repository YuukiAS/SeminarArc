# Result 0.5.x_blackbox_acceptance_harness

status: completed

## 执行摘要

- 已创建长期一键入口：`scripts/run-blackbox-acceptance.ps1`。
- harness 以安装后的 `com.yuukias.seminararc.internal` 作为黑盒，通过 Windows Emulator `emulator-5554` 的 UI tree、`adb shell input`、screenshot、scoped logcat 和 package/activity 操作完成验收。
- 本轮 black-box run：`private/blackbox-acceptance/20260913T010316Z`。
- 自动执行 step 数：36。
- B01 / B02 / B03 / B08 均为 PASS。
- 未操作 GM1910 / `8cc54656`；本轮 ADB inventory 仅看到 `emulator-5554 device`。

## 读取文件

- `AGENTS.md`
- `prompts/AGENT_RULES.md`
- `prompts/CHATGPT_RULES.md`
- `docs/DEVICE_TESTING.md`
- `docs/CI_POLICY.md`
- `docs/plans/0.5.x-blackbox-acceptance-plan.md`
- `prompts/tasks/post_0.3_autonomous_roadmap_master_result.md`
- `README.md`
- 当前 navigation / library / editor / detail / active session UI 和相关 ViewModel / use case / androidTest 代码。

## 修改文件

- `.gitignore`
  - 忽略 `private/blackbox-acceptance/` 大体积本地运行证据。
- `scripts/run-blackbox-acceptance.ps1`
  - 新增 Windows Emulator-only black-box harness。
- `app/src/main/java/com/yuukias/seminararc/domain/usecase/EndSeminarUseCase.kt`
  - 修复 photos-only session 结束时无 open recording rows 仍发送 recording service stop 的崩溃路径。
- `app/src/main/java/com/yuukias/seminararc/recording/service/RecordingServiceStarter.kt`
  - stop intent 改为普通 `startService`，避免 stop 命令误触发 foreground-service start timeout。
- `app/src/test/java/com/yuukias/seminararc/recording/EndSeminarUseCaseTest.kt`
  - 新增 photos-only end 回归测试，并保留 open recording rows 时仍发送 service stop 的断言。

## Scenario 结果

| Scenario | 结果 | 证据 |
| --- | --- | --- |
| B01 Fresh install / empty library | PASS | empty library、Create seminar entry、screenshot/UI tree、crash buffer |
| B02 Seminar CRUD / persistence | PASS | title required validation、create、search、reopen、edit、force-stop/relaunch persistence |
| B03 Session lifecycle Emulator-safe | PASS | microphone permission denied path、photos-only active session、NOTE / QUESTION / MARK、End Seminar、completed persistence |
| B08 Recovery / cleanup | PASS | force-stop/relaunch recovery、UI delete、post-delete relaunch empty library |

## 运行命令

- `git fetch origin`
- `git status --short --branch`
- `git pull --ff-only origin main`
- `D:\Android\Sdk\platform-tools\adb.exe devices -l`
- `.\gradlew.bat :app:testDebugUnitTest --tests com.yuukias.seminararc.recording.EndSeminarUseCaseTest --console=plain`
- `.\gradlew.bat :app:assembleInternal --console=plain`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-blackbox-acceptance.ps1 -SkipBuild`
- `.\gradlew.bat testDebugUnitTest assembleInternal lintDebug --console=plain`
- `.\gradlew.bat connectedDebugAndroidTest --console=plain`

## 测试结果

- 聚焦 JVM 回归：PASS。
- `testDebugUnitTest assembleInternal lintDebug`：PASS。
- black-box harness：PASS，36 steps，evidence root `private/blackbox-acceptance/20260913T010316Z`。
- `connectedDebugAndroidTest`：PASS，Pixel_8(AVD) / API 36，24 tests，0 failed。

## 修复的真实 bug

- B03 在 photos-only active session 中执行 End Seminar 时触发真实崩溃：
  - `ForegroundServiceDidNotStartInTimeException`
  - root cause：没有 live recorder 的 photos-only session 仍向 `SeminarRecordingService` 发送 foreground stop intent。
- 修复后 B03 black-box End Seminar 和 completed persistence 通过，crash buffer 无 app crash。

## 当前仍无法自动覆盖的项目

- 真实麦克风音质。
- 真实 CameraX 拍投影/PPT 的质量、方向和厂商相机行为。
- 厂商 ROM 后台/锁屏对长录音的影响。
- 一次真实低风险 seminar 的连续使用手感。
- 主观 UI/UX 判断。

## git diff 摘要

- 新增 `scripts/run-blackbox-acceptance.ps1`。
- 新增 `.gitignore` 私有证据目录规则。
- 修复 End Seminar photos-only foreground-service crash。
- 增加对应 JVM 回归测试。

## 需要人工批准的事项

- 无。

## 下一步建议

- 可以进入下一张 `0.5.x_blackbox_research_flows_task.md`。
- 本轮包含真实 dogfood blocker 修复，但仍主要是 harness/测试与局部崩溃修复；是否准备 `0.5.1-alpha` 默认为 NO，除非用户希望把该 crash fix 作为可安装修复版发布。
