---
task_key: 0.5.x_formula_region_edit_selection
completed_at: 2026-09-09
status: completed
owner: codex
---

# Result

Added saved formula region edit/update foundation.

## Changes

- Added `UpdateFormulaRegionInput` and `FormulaRepository.updateRegion`.
- `FormulaRepositoryImpl.updateRegion` updates crop/label/rotation timestamp while preserving existing region identity, seminar ownership, source asset id and source photo path.
- `ReconstructionWorkspaceViewModel` now exposes `onUpdateFormulaRegion` with success/failure snackbar feedback.
- Reconstruction workspace saved regions now expose `Edit crop`; editing loads that region into the shared draft fields, switches the save button to `Update formula region`, and offers `Cancel formula edit`.
- Existing formula results remain attached to the same region id after crop update.
- Added Compose fixture coverage for edit/update actions.
- Updated README, TODO, changelog, architecture and privacy notes.

## Validation

- Local Kotlin and androidTest compile passed:
  `testDebugUnitTest compileDebugAndroidTestKotlin`
- Lint and internal APK build passed:
  `lintDebug assembleInternal`
- Internal APK signature verified with certificate SHA-256:
  `7555bf92fb796c6209f353f66c156c6f65e0312f18ee918557b572014b2db28a`
- Internal APK SHA-256:
  `617B6123200E59D2DD5D8C8FA957B69D97499854E36E40F45E4532E0D554FD60`
- D-drive cache check passed: `C:\.gradle` and `C:\.android` were absent; `.gradle-user-home` and `.android-user-home` existed under the D-drive checkout.
- Secret scan found no matches outside ignored build/cache/schema/local files.

## Boundaries

- No persisted drag resize handles were added.
- No automatic provider rerun, live formula OCR provider, cloud upload, credential storage, backend, Zotero sync or Notion live upload was added.
- Connected Emulator execution was not run because Windows ADB isolation is currently unsafe when the protected physical serial is visible.
