---
id: 0.2.x_room_migration_and_backfill
status: completed
owner: codex
completed: 2026-08-30
---

# 0.2.x Room Migration And Backfill Result

## 完成内容

- `AppDatabase` 从 version `2` 升级到 version `3`。
- 新增 `MIGRATION_2_3`，创建 `seminar_assets`、`processing_jobs`、`ocr_results`、`tags`、`asset_tags`。
- 生成 Room schema `app/schemas/com.yuukias.seminararc.data.local.AppDatabase/3.json`。
- `MIGRATION_2_3` seed 7 个系统 tag。
- `MIGRATION_2_3` 从旧字段 backfill:
  - `seminars.abstractPdfPath` -> `ABSTRACT_PDF`
  - `recordings.filePath` -> `RECORDING`
  - `timeline_events.photoPath` -> `PHOTO_ORIGINAL`
  - `audio_clips.filePath` -> `AUDIO_CLIP`

## 保护

- migration 不移动、不删除、不重写现有媒体文件。
- 现有 0.1.x 表和路径字段保留。
- 不使用 `fallbackToDestructiveMigration()`。

## 验证

- `AppDatabaseMigrationTest.migrate2To3_backfillsAssetsAndSystemTags`
- WSL headless `testDebugUnitTest assembleDebug lintDebug` 通过。
