---
task_key: 0.5.x_formula_result_export
completed_at: 2026-09-08
status: completed
owner: codex
---

# Result

Added local formula result export support.

## Changes

- Extended `SeminarExportDocument` with `ExportFormulaItem`.
- Export assembly now loads formula regions/results and includes only latest READY non-empty LaTeX per region.
- Formula source photos are added to ZIP media assets under `media/formulas/` when readable.
- Markdown export now includes a `Formula Results` section with fenced LaTeX, provider/version, confidence, edited flag, crop, source photo and provenance.
- Notion-ready export now includes formula headings and LaTeX code blocks.
- Added/updated JVM coverage for assembler, Markdown renderer and Notion-ready renderer behavior.

## Validation

- Targeted JVM tests passed:
  `testDebugUnitTest --tests com.yuukias.seminararc.domain.SeminarExportAssemblerTest --tests com.yuukias.seminararc.domain.SeminarMarkdownRendererTest --tests com.yuukias.seminararc.domain.SeminarNotionReadyRendererTest`
- Full local JVM / androidTest compile gate passed:
  `testDebugUnitTest compileDebugAndroidTestKotlin`
- Lint and internal APK build passed:
  `lintDebug assembleInternal`
- Internal APK signature verified with certificate SHA-256:
  `7555bf92fb796c6209f353f66c156c6f65e0312f18ee918557b572014b2db28a`
- Internal APK SHA-256:
  `6958F4B2CBFA3B99889B2579670B696EF791410FEAA063A5187D21A0A5EAC880`
- D-drive cache check passed: `C:\.gradle` and `C:\.android` were absent; `.gradle-user-home` and `.android-user-home` existed under the D-drive checkout.
- Secret scan found only boundary documentation, existing token-check test text, signing property key names and documented local secret-store paths; no keystore material, password value, API token or private key was found.

## Boundaries

- No live formula OCR provider, cloud upload, credential storage, backend, Zotero sync or Notion live upload was added.
- Connected Emulator execution was not run because Windows ADB isolation is currently unsafe when the protected physical serial is visible.
