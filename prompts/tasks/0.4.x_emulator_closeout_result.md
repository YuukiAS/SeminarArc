# 0.4.x Emulator Closeout Result

## Status

`CONNECTED_GATE_NOT_RUN`

## Evidence

- Working tree was clean on `main...origin/main` after the last `0.4.x` local-safe implementation commit.
- Most recent headless verification passed with D-drive local homes:
  - `.\gradlew.bat --no-daemon testDebugUnitTest compileDebugAndroidTestKotlin`: passed.
  - `.\gradlew.bat --no-daemon lintDebug assembleInternal`: passed.
  - `D:\Android\Sdk\build-tools\36.0.0\apksigner.bat verify --verbose --print-certs app\build\outputs\apk\internal\app-internal.apk`: passed.
  - Internal APK SHA-256: `1d71288ed1577c534ef9b7e317e30baba7bc17f1acc3aa294aa37d5cacac9632`.
  - Internal APK size: `60469410` bytes.
- Sandbox read-only Windows ADB preflight did not reach a device list. `D:\Android\Sdk\platform-tools\adb.exe devices -l` failed with:
  - `Cannot mkdir '\.android': Permission denied`
- Retried in a non-sandbox PowerShell process with temporary D-drive ADB home variables:
  - `USERPROFILE=D:\Code\SeminarArc-emulator\.android-user-home`
  - `HOME=D:\Code\SeminarArc-emulator\.android-user-home`
  - `ANDROID_SDK_HOME=D:\Code\SeminarArc-emulator\.android-user-home`
- That non-sandbox preflight reached the device list and showed both:
  - `8cc54656 unauthorized`
  - `emulator-5554 device product:sdk_gphone64_x86_64 model:sdk_gphone64_x86_64 device:emu64xa`

## Decision

Connected Emulator closeout cannot safely proceed in this session because Windows ADB can see protected physical serial `8cc54656` in addition to `emulator-5554`.

## Safety Boundary

- No APK was installed.
- No connected Android test was run.
- No app launch automation was attempted.
- No `adb kill-server`, transport reset, reconnect, usbipd command, physical-device command, or WSL restart was run.

## Next Recovery

Disconnect or otherwise isolate the protected physical device from Windows ADB without using Codex to run transport-changing commands. Then rerun the non-sandbox D-home ADB preflight; proceed to connected Emulator tests only if the output contains expected `emulator-*` devices and no protected physical serial `8cc54656`.
