# 0.4.x Notion-Ready Export Contract Result

## Status

Implemented.

## Changes

- Added `SeminarNotionReadyRenderer`.
- Added provider-neutral `NotionReadyExportDocument`, `NotionReadyBlock`, and `NotionReadyBlockType`.
- The renderer maps seminar metadata, abstract, recording summary, transcript review content, timeline windows, generated summary drafts, brief sections, confirmed references, key slides, timeline items, skipped media, and local relative media paths.
- Added a Markdown preview renderer for local inspection of the block-like contract.
- Added JVM coverage proving transcript/summary/media content is represented and the preview states that it is not uploaded.

## Privacy / Scope Boundary

- The contract is local-only.
- It carries text and local relative media paths from the already assembled export document.
- No Notion OAuth, Notion API client, token storage, file upload, backend, private credential, or recurring-cost service was added.

## Verification

- `.\gradlew.bat --no-daemon testDebugUnitTest compileDebugAndroidTestKotlin`: passed.
- `.\gradlew.bat --no-daemon lintDebug assembleInternal`: passed.
- `D:\Android\Sdk\build-tools\36.0.0\apksigner.bat verify --verbose --print-certs app\build\outputs\apk\internal\app-internal.apk`: passed.
- Internal APK SHA-256: `398e40812b97aed6dddaaf47d4eab49cc7995f87b44729881bf88d6db1f8d06a`.
- Internal APK size: `60469410` bytes.
- Refreshed ignored local staging artifact `release\SeminarArc-0.3.1-internal.1.apk` and `.sha256`.

## Remaining Gate

- Connected Android tests and device installation remain intentionally skipped while Windows ADB can see protected physical device serial `8cc54656`.
