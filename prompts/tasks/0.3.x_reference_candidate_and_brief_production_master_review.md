---
id: 0.3.x_reference_candidate_and_brief_production_master
status: OPEN_NEXT_TASK
reviewed_at: 2026-09-08
reviewer: chatgpt
---

# 0.3.x Reference Candidate + Seminar Brief Production Review

## Conclusion

`0.3.x COMPLETE` 结论成立。

当前证据足以进入下一 roadmap stage：

- Room v4 / `MIGRATION_3_4` 已实现；
- reference evidence / lookup attempt / canonical candidate / provider source / Seminar Brief 模型已落地；
- Crossref / OpenAlex / DataCite keyless provider 已实现；
- deterministic evidence extraction、`reference-match-v1` ranking、confidence band、dedup/provenance 已实现；
- Candidate Review / confirm/reject/reopen / editable Seminar Brief 已实现；
- confirmed references + Brief/key slides 已进入 Markdown/ZIP；
- Windows Emulator connected suite 18 tests / 0 failures；
- v1->v2->v3->v4 migration chain、0.1.x/0.2.x regression、OCR/image/WorkManager regression 均保留；
- live provider smoke 仅使用固定公共 fixture；
- GM1910 未参与日常 connected/instrumentation；
- 当前仍无 transcription / AI summary / Notion / formula OCR / cloud upload / ads/payment。

## Release/distribution gap

当前产品功能已经值得开始 personal dogfood，但仓库还没有稳定的“下载 APK -> 后续原地更新”的分发合同：

- `.github/workflows/android.yml` 只 build/test，不 upload APK artifact；
- `app/build.gradle.kts` version metadata 仍滞后于当前功能线；
- stable internal signing 尚未形成明确 channel。

因此下一步不应直接跳进大规模 `0.4.x` production；先完成一个小的 `0.3.1-internal` distribution milestone，再沿 roadmap 自动推进。

## Next task

立即执行：

`prompts/tasks/post_0.3_autonomous_roadmap_master_task.md`

该 master 已明确授权：

1. 先完成 `0.3.1_internal_dogfood_distribution_task.md`；
2. 再做 `0.4.x` readiness -> safe production -> Emulator closeout；
3. 再做 `0.5.x` readiness -> safe production -> Emulator closeout；
4. 每个 accepted milestone 生成 internal APK artifact（在 signing/distribution 条件允许时）；
5. 到 `0.9.x` 只生成 release readiness report，生产签名/Play/public release 必须人工批准。

## Review state

`OPEN_NEXT_TASK`
