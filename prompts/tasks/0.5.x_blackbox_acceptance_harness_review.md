---
id: 0.5.x_blackbox_acceptance_harness_review
status: OPEN_NEXT_TASK
reviewed_at: 2026-09-13
reviewer: chatgpt
---

# 0.5.x Black-box Acceptance Harness Review

## 结论

`0.5.x_blackbox_acceptance_harness_task.md` 已完成，证据足以进入下一张 research-flow black-box task。

Review 状态：`OPEN_NEXT_TASK`。

## 完成度

第一阶段目标全部闭环：

- B01 Fresh install / empty library：PASS；
- B02 Seminar CRUD / persistence：PASS；
- B03 Session lifecycle Emulator-safe：PASS；
- B08 Recovery / cleanup：PASS；
- 自动执行 36 steps；
- 已建立长期入口 `scripts/run-blackbox-acceptance.ps1`；
- 运行证据落在 `private/blackbox-acceptance/<run-id>/`，并已通过 `.gitignore` 隔离大体积 evidence；
- unit/build/lint 与现有 Emulator instrumentation regression 均继续 PASS。

## 关键价值

本轮不是“为了自动化而自动化”，而是真正发现并修复了用户路径中的 production bug：

photos-only seminar 在没有 live recorder 时结束，仍会向 recording foreground service 发送 stop intent，导致 `ForegroundServiceDidNotStartInTimeException`。

修复后：

- photos-only End Seminar 黑盒流程通过；
- completed persistence 通过；
- crash buffer 无对应 app crash；
- focused JVM regression 已补齐。

这证明 package-level black-box acceptance 对现有白盒/Compose/instrumentation coverage 有独立价值，应继续扩展。

## 安全 / 越权检查

PASS。

- 本轮设备 inventory 仅见 `emulator-5554`；
- 未操作 GM1910 / `8cc54656`；
- 黑盒步骤通过 UI tree / adb input / screenshot / logcat / package/activity surface 驱动；
- 没有通过直接写 Room 或 test-only production backdoor 伪造用户成功；
- 未创建无意义 version tag / Release；
- 未进入真实 ASR、LLM、Notion OAuth、Mathpix、Google Play 等越界功能。

## 下一阶段

进入：

`prompts/tasks/0.5.x_blackbox_research_flows_task.md`

下一阶段只扩展现有 harness 到：

- B04 Reconstruction；
- B05 Reference workflow；
- B06 Transcript / Summary local-safe flow；
- B07 Formula / research export。

仍然不把 screenshot golden、Monkey、多设备和 Firebase Test Lab 混入同一 task。完成 B04-B07 后再单独进入 visual regression。

## Release 判断

当前不因为第一阶段 harness 本身立即发布新 alpha。

虽然修复了真实 crash，但应先让 B04-B07 完整跑一轮，确认是否还有其他 dogfood blocker，再统一判断是否值得发布 `0.5.1-alpha.*`，避免为了单一修复连续制造多个 alpha。
