# 0.4.x Summary Draft Edit Result

## Status

Implemented.

## Changes

- `TranscriptRepository` now exposes a local `editSummaryDraft` boundary.
- `TranscriptRepositoryImpl` edits one `summary_drafts` row without changing provider id, input fingerprint, provenance JSON, or created time.
- Saved edits trim field text, clear stale provider error text, refresh `updatedAt`, and mark the row as `DRAFT`.
- `TranscriptReviewViewModel` now keeps per-draft field edits and saves them through the repository.
- `TranscriptReviewScreen` now renders editable fields for generated summary draft sections.
- Edited drafts remain compatible with existing Markdown/ZIP export and Notion-ready export contract paths.

## Privacy / Scope Boundary

- Summary draft edits are local Room updates.
- No ASR provider, LLM provider, network, Notion OAuth/upload, backend, token storage, private credential, or recurring-cost service is added.

## Verification

- `.\gradlew.bat --no-daemon testDebugUnitTest compileDebugAndroidTestKotlin`: passed.
- `.\gradlew.bat --no-daemon lintDebug assembleInternal`: passed.
- `D:\Android\Sdk\build-tools\36.0.0\apksigner.bat verify --verbose --print-certs app\build\outputs\apk\internal\app-internal.apk`: passed.
- Internal APK SHA-256: `44f42edaf31e7a5325cbe8a9e2e8422f1794be7a3a9c7383ed9aa758ba19238d`.
- Internal APK size: `60485794` bytes.
- Refreshed ignored local staging artifact `release\SeminarArc-0.3.1-internal.1.apk` and `.sha256`.

## Remaining Gate

- Connected Android tests and device installation remain intentionally skipped while Windows ADB can see protected physical device serial `8cc54656`.
