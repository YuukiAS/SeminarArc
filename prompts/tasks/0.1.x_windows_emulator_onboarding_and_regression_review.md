---
id: 0.1.x_windows_emulator_onboarding_and_regression_review
status: GO
reviewer: chatgpt
created: 2026-08-30
---

# 0.1.x Windows Emulator Onboarding And Regression Review

## 结论

`GO`。

本 task 的目标已经完成，Windows Emulator 现可作为 SeminarArc 默认的 connected / instrumentation / Room migration / Compose UI regression 环境。

这同时意味着此前的 `EMULATOR_INFRA_NEEDS_FIX` 已被 Windows 原生执行路径正式消除，不再是开发 blocker。

## 完成证据

- Windows SDK：`D:\Android\Sdk`。
- Windows mirror：`D:\Code\SeminarArc-emulator` 已建立并验证。
- AVD：`Pixel_8`。
- Emulator serial：`emulator-5554`。
- API / ABI：`36` / `x86_64`。
- WHPX acceleration：可用。
- Windows ADB gate 只看到 `emulator-5554`，未看到 GM1910 / `8cc54656`。
- `testDebugUnitTest`：PASS。
- `assembleDebug`：PASS。
- `lintDebug`：PASS。
- `connectedDebugAndroidTest`：PASS，6 tests / 0 failures / 0 errors / 0 skipped。
- `MIGRATION_1_2` instrumentation 已在 Emulator 上真实 install / execute / PASS。
- Emulator smoke 已覆盖 list、create/open seminar、detail、Active Session 基础路径、timeline、export UI、Room reopen persistence。
- GM1910 在 Windows 原生 task 开始和结束的 `usbipd list` 均保持 `Attached`，本轮没有对物理真机执行 ADB/usbipd transport 操作。

## 本轮发现并修复的问题

- androidTest 未包含 Room schema assets，导致 migration instrumentation 失败；已修复。
- `SeminarDetailViewModel.onOpenTimelineClicked()` 的无 buffer `tryEmit()` 会丢导航事件；已改为 coroutine `emit()` 并补测试。
- Detail 页面不可滚动导致 Export/Delete 在 Emulator 视口不可达；已增加滚动能力并补 Compose instrumentation 断言。
- Windows unit test 中存在硬编码 Unix 路径；已改为平台无关路径处理。

这些均属于真实 app/test 问题，不是为了让测试通过而削弱断言。

## 当前项目状态

- `0.1.x` 功能实现线：`COMPLETE`。
- WSL headless gate：`PASS`。
- Windows Emulator regression：`EMULATOR_REGRESSION_PASS`。
- 物理 GM1910：仅保留未来明确授权的 hardware checkpoint，不再承担日常 connected/instrumentation 测试。
- 真机全量 acceptance：`DEFERRED BY TEST STRATEGY`。
- deletion cleanup 真机验收：`DEFERRED / REQUIRES EXPLICIT HUMAN APPROVAL`。

因此当前不是 `FULL RELEASE ACCEPTANCE`，但也不存在阻塞日常开发的未完成 0.1.x 工程 gate。

## 后续状态

默认测试分工正式生效：

- WSL canonical repo：开发、JVM/headless、build、lint。
- Windows Emulator mirror：connected Android tests、Compose instrumentation、Room migration instrumentation、可重复 UI regression。
- GM1910：only explicit hardware checkpoint。

后续可以进入下一阶段规划；如进入 `0.2.x`，应新建独立 task，而不是继续恢复旧 physical-device acceptance goal。
