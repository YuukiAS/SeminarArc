---
id: 0.1.x_emulator_onboarding_and_regression
status: EMULATOR_INFRA_NEEDS_FIX
owner: codex
completed: 2026-08-30
---

# 0.1.x Emulator Onboarding And Regression Result

## 执行摘要

- 已按要求先在 canonical WSL repo 执行 `git pull --ff-only origin main`，fast-forward 到 `d111368`。
- Physical device preflight 只读检查结果：`PHYSICAL_DEVICE_CHANNEL_UNAVAILABLE`。WSL `adb devices -l` 没有列出 `8cc54656` 或任何设备；未尝试恢复连接，未操作 GM1910。
- Physical device postflight 只读检查结果：`PHYSICAL_TRANSPORT_UNCHANGED / ONLINE_AT_END`。结束时 WSL `adb devices -l` 显示 `8cc54656 device usb:1-1 product:OnePlus7Pro_CH model:GM1910 device:OnePlus7Pro transport_id:2`；此后未继续操作真机。
- Windows interop 当前不可用：`powershell.exe` 不在 WSL PATH；直接执行 `/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe` 和 `/mnt/c/Windows/System32/cmd.exe` 均返回 `Invalid argument`。
- `/mnt/d` 当前以 `ro` 挂载，无法从本 WSL session 写入 `D:\Code\SeminarArc-emulator`。
- 因此本轮无法验证 Windows SDK、AVD、Windows ADB 隔离、JBR、Windows mirror、Windows Gradle 或 Emulator connected tests。
- WSL canonical headless gate 仍通过：`./gradlew testDebugUnitTest assembleDebug lintDebug`。

## 读取文件

- `AGENTS.md`
- `docs/DEVICE_TESTING.md`
- `prompts/AGENT_RULES.md`
- `prompts/tasks/0.1.x_local_mvp_completion_master_result.md`
- `prompts/tasks/0.1.x_local_mvp_device_acceptance_resume_task.md`
- `prompts/tasks/0.1.x_emulator_onboarding_and_regression_task.md`
- `.agents/skills/android-lead/SKILL.md`
- `.agents/skills/compose-expert/SKILL.md`

## 修改文件

- `AGENTS.md`
  - 记录当前 WSL Windows interop 不可执行和 `/mnt/d` 只读的环境事实。
- `docs/DEVICE_TESTING.md`
  - 记录本次 Emulator onboarding 未能进入 Windows SDK/AVD 检查的具体原因。
- `prompts/tasks/0.1.x_local_mvp_completion_master_result.md`
  - 将 Emulator regression 状态更新为 `PENDING / EMULATOR_INFRA_NEEDS_FIX`。
- `prompts/tasks/0.1.x_emulator_onboarding_and_regression_result.md`
  - 新增本结果记录。

## 运行命令

- `git pull --ff-only origin main`
- `/home/yuukias/Android/Sdk/platform-tools/adb devices -l`
- `powershell.exe -NoProfile -Command "Test-Path 'D:\Android\Sdk\platform-tools\adb.exe'"`
- `/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -Command "Test-Path 'D:\Android\Sdk\platform-tools\adb.exe'"`
- `/mnt/c/Windows/System32/cmd.exe /C ver`
- `findmnt -no OPTIONS /mnt/d`
- `./gradlew testDebugUnitTest assembleDebug lintDebug`
- `/home/yuukias/Android/Sdk/platform-tools/adb devices -l` postflight

## 测试结果

- WSL `./gradlew testDebugUnitTest assembleDebug lintDebug`：通过。
- Windows `testDebugUnitTest` / `assembleDebug` / `lintDebug`：未运行，原因是 Windows interop 不可执行。
- Emulator `connectedDebugAndroidTest`：未运行，原因是无法检查 Windows ADB 是否只看到 emulator device。
- Emulator smoke：未运行，原因是无法从当前 WSL session 操作 Windows Emulator。

## 失败信息

- `powershell.exe`: `/bin/bash: line 1: powershell.exe: command not found`
- `/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe`: `Invalid argument`
- `/mnt/c/Windows/System32/cmd.exe`: `Invalid argument`
- `/mnt/d` mount options include `ro`。

## Git diff 摘要

- 仅更新测试策略/结果文档；未改 app 产品代码。
- 未提交 `local.properties`、Gradle cache、AVD image、APK、录音、照片、导出 ZIP 或 `.ai-bridge-backups/`。

## 需要人工批准或外部处理的事项

- 需要用户恢复 Windows interop 或在 Windows 侧手动准备 `D:\Code\SeminarArc-emulator` 后重跑此 task。
- 不需要恢复旧 physical-device acceptance goal。
- 不需要也不允许通过 `wsl --shutdown`、usbipd、ADB transport 操作或真机连接恢复来绕过本问题。

## 下一步建议

- 在 Windows PowerShell 中确认 `D:\Android\Sdk\platform-tools\adb.exe`、`D:\Android\Sdk\emulator\emulator.exe`、已启动 Emulator 和 `D:\Code\SeminarArc-emulator` 可写。
- 恢复 WSL 调用 Windows exe 的 interop 能力，或把 Windows mirror 测试命令交给一个 Windows-native Codex/task 执行。
