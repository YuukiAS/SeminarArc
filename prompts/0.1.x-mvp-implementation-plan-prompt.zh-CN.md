你现在负责为当前仓库 `YuukiAS/SeminarArc` 制定一个 **原生 Android MVP 实现计划**，暂时不要直接开始大规模编码实现。

## 目标

请基于以下三类约束，输出一个高质量、可执行、分阶段的实现 plan：

1. 产品与工程真约束：`TODO.md`
2. 当前既有总体计划：`docs/plans/0.1.x-mvp-implementation-plan.md`
3. 当前设计交付：
   - `design/README.md`
   - `design/UI_SPEC.md`
   - `design/USER_FLOW.md`
   - `design/DESIGN_TOKENS.md`
   - 必要时可补读 `design/screens/*.png` 与 `design/states/*.png`

同时遵守：

- `AGENTS.md`
- `.agents/skills/android-lead/SKILL.md`
- `.agents/skills/compose-expert/SKILL.md`
- 对应 references 中与当前计划直接相关的内容

## 这次任务的边界

这次任务是 **制定实现计划**，不是立即完成全部实现。

你可以：

- 阅读仓库现状
- 分析当前工程骨架
- 梳理设计与 `TODO.md` 的映射关系
- 识别实现阶段顺序、依赖、风险、验证点
- 给出推荐分批推进方案

你现在不要做的事：

- 不要直接进入完整功能开发
- 不要假设已经进入真机调试阶段
- 不要把设计图当成静态页面任务处理
- 不要无视 `TODO.md` 先自由发挥做 UI
- 不要在本轮为了凑进度做“假实现”“假按钮”“内存假数据完成态”

## 特别要求

### 1. 先制定计划，再执行

你这次的主要产物是一个 **实现计划**，而不是代码实现总结。

### 2. 计划必须考虑 TODO.md 的真实约束

尤其要显式纳入这些实现难点，而不是只列页面：

- 所有素材必须强归属到同一 `seminarId`
- 同时只能有一个 active seminar
- 录音依赖 foreground service，而不是 Activity 内状态
- timeline 必须绑定 recording offset
- clip 有 `PENDING / PROCESSING / READY / FAILED`
- 权限拒绝、文件缺失、处理中、恢复流都必须有真实状态路径
- 不能只做静态 Compose 界面冒充 MVP

### 3. 暂时不接入真机

本轮 plan 必须明确写出：

- 当前阶段先不接入真机
- 可以先以工程结构、真实数据流、模拟器可运行、测试基础为目标
- 真机验收属于后续阶段，而不是本轮计划输出的前置条件

### 4. 计划执行时再落盘

你这次先在回复中给出完整 plan。

**只有当用户明确要求你按这个 plan 开始执行实现时，才需要在 `docs/plans/` 下生成新的执行计划文件。**

也就是说：

- 当前回合：先产出 plan 内容
- 后续执行回合：先把该计划落盘到 `docs/plans/`，再开始实现

## 你需要回答的内容

请输出一份中文 plan，至少包含以下部分：

### A. 上下文理解
简要说明你如何理解 SeminarArc 的 MVP、本地优先约束、设计方向 A（Academic Archive）以及当前实现边界。

### B. 推荐实现顺序
必须给出你推荐的分阶段顺序，并解释为什么这样排，而不是先画完 UI。

建议你至少覆盖这些层次，但可以根据仓库现状调整命名：

- Theme / design tokens / app shell
- Data model / Room / storage / repository
- Seminar CRUD / PDF import / list-detail-editor
- Recording service / active seminar invariant / recovery
- Timeline events / note / question / photo capture
- Clip generation / fallback / retry
- Testing / docs / CI / hardening

### C. 每阶段交付物
对每个阶段说明：

- 实现目标
- 关键文件或模块
- 与 `TODO.md` 的对应关系
- 与设计稿的对应关系
- 验证方式
- 阶段风险

### D. 当前不建议做的事
说明哪些东西现在不应该提前做，例如：

- 真机验收
- AI/转录/OCR
- 为了好看先做大量无数据支撑的 UI
- 假装完成 clip/recording 能力

### E. 执行建议
最后给出一句明确建议：

- 如果用户确认采用该计划，下一轮应先在 `docs/plans/` 新建一个执行计划文件
- 然后再按该计划开始真实实现

## 输出风格要求

- 用中文
- 不空泛
- 不要只复述 `TODO.md`
- 必须体现你理解了设计交付和真实 Android 工程约束
- 必须给出“为什么这个顺序更稳”的判断
- 计划要足够具体，后续 agent 可以据此直接进入实施
